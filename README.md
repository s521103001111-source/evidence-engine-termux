# Evidence Engine V2 (Termux Mobile Baseline)

Deterministic evidence-first auditing engine optimized for single-device execution on Vivo X300 Pro via Termux.

## Technical Highlights
* **Zero External Dependencies**: Built entirely using Python 3 Standard Library (
mkdir -p .github/workflows

cat << 'EOF' > .gitignore
__pycache__/
*.py[cod]
*.db
*.db-wal
*.db-shm
*.jsonl
*.lock
